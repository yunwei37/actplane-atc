%%%%%%%%%%%%Outline%%%%%%%%%%%%%%%%%%

%%%
% message<opening: \lang introduces two representations — formal verification proves they are equivalent>
% the proof sequence defines what the \lang opcode means (verifier-visible semantics)
% the native emit defines how it executes (architecture-specific fast path)
% this section proves the two are consistent on observable program state
% §5.1 defines the correctness criteria, §5.2 proves the whole-program refinement theorem

%%% Section 5.1 Correctness Criteria

%%%
% message<local refinement: native emit must agree with proof sequence on observable state>
% program state: registers R and memory M
% observable state at a \lang site: only live-out registers and memory (dead registers may differ)
% local refinement condition: for every valid initial state, π(Exec_native) = π(Exec_proof)
% π is the projection to observable state (LiveOut registers + memory)

%%%
% message<three preconditions must hold for local refinement>
% Valid(payload): payload is well-formed for this descriptor (e.g., shift amount in range)
% Admissible(site, payload): site-specific conditions hold (e.g., temporary register is dead after site)
% VerifierSafe(state, site): verifier has proven memory accesses in the proof sequence are safe
% the LLVM backend enforces Valid and Admissible at emit time; the verifier enforces VerifierSafe

%%% Section 5.2 Whole-Program Refinement

%%%
% message<local refinement composes to whole-program refinement>
% theorem: if every \lang site satisfies local refinement, then P_jit agrees with P_low on (r0, Memory)
% P_low = the lowered program (all \lang sites replaced with proof sequences) that the verifier checked
% P_jit = the JIT-compiled program with native emit for \lang sites

%%%
% message<proof sketch: trace simulation between P_jit and P_low>
% between \lang sites: identical BPF instructions, so state stays synchronized
% at each \lang site: local refinement guarantees observable output matches
% induction on the number of \lang site entries along the execution trace

%%%
% message<trusted computing base>
% kernel verifier: proves proof-sequence safety (memory safety, type safety, termination)
% kernel modules: provide instantiate_insn() and native emit — must be deterministic and pure
% LLVM backend: enforces Valid and Admissible at emit time — outside TCB for safety, inside TCB for correctness
% a bug in the LLVM backend cannot crash the kernel (verifier re-checks), but can produce semantically incorrect programs

%%%%%%%%%%%%Outline%%%%%%%%%%%%%%%%%%

\section{Formal Verification of \lang Correctness}
\label{sec:formal-verification}